func int volume(int length, int width, int height)
    int lw;
    lw = length * width;
    return lw * height;
endfunc

func int roomVolume()
    return volume(14, 13, 8) + volume(6, 10, 8);
endfunc
