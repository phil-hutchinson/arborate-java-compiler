func int double(int a)
    return a * 2;
endfunc

func int test()
    int a;
    int b;
    a = double(2 + 5);
    b = double((1 + 3) / (2 * 2));
    b = b + a;
    return double(b);
endfunc
